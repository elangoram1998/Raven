import { IsSamePasswordDirective } from './is-same-password.directive';

describe('IsSamePasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new IsSamePasswordDirective();
    expect(directive).toBeTruthy();
  });
});
